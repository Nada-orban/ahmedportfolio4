import Image from 'next/image'
import Head from 'next/head'
import { Inter } from 'next/font/google'
import About from '../../Components/About'
import Skill from '../../Components/Skill'
import Project from '../../Components/Project'
import { BsGithub } from 'react-icons/bs'
import { BsFillFileEarmarkPdfFill } from 'react-icons/bs'
import Link from 'next/link'
import { projectsdata } from '../../data'

const inter = Inter({ subsets: ['latin'] })

export default function Home({ projects }) {
  return (
    <>
      <Head>
        <title>My Portfolio</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="%PUBLIC_URL%/logonada.png" />
        <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet"></link>
        <link rel="stylesheet" href="/path/to/folder/css/academicons.min.css" />
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/jpswalsh/academicons@1/css/academicons.min.css"></link>



      </Head>
      <div className=''>

        <About />
        <Skill />

        {/* ****************** projects ***************/}
        <div id="project" className='container mx-auto  text-start sm:h-screen'>
          <div className='grid  grid-cols-1  sm:grid-cols-3 px-5 py-16  mt-10 sm:grid-flow-col gap-4'>
            <div>
              <h1 className='font-bold  font-serif'>Projects</h1>
            </div>
            <div className='flex flex-col col-span-2 gap-4  pt-5'>
              {projects.map(project => (
                <Project key={project.id} {...project} />

              ))}
            </div>
          </div>
        </div >
        {/* end project */}



      </div>


    </>

  )
}

export const getStaticProps = async () => {
  // const res = await fetch("data.projects?_limit=2");
  // const data = await res.json();

  return {
    props: {
      projects: projectsdata,
    }
  }
}


